[tool.black]
skip-string-normalization = true

[tool.pylint.'MESSAGES CONTROL']
disable = ["C", "R", "W", "E1101"]

[tool.isort]
profile = "black"

[tool.coverage.run]
source = ["planetmapper"]
branch = true